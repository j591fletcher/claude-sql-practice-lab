# SQL Practice Web App

A web app created with Claude Code that allows the user to practice SQL. The SQL is based on free databases provided from Mode and problems are generated by Claude based on SQL topic.
