# Data Folder

Place your downloaded SQLite database files here with extensions `.db`, `.sqlite`, or `.sqlite3`.

Example:

- `sample.db`
- `sales.sqlite`

The web app loads available database files from this folder and exposes them to the UI.

If you want Claude-generated problems, add your Claude API key to `.env` using the `.env.example` template.
